[package]
name = "configurator"
version = "0.1.0"
edition = "2024"

[dependencies]
config = { path = "../config" }

anyhow = { version = "1.0.104" }
clap = { version = "4.4", features = ["derive"] }

[dev-dependencies]
tempfile = { version = "3.27.0" }