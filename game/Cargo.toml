[package]
name = "game"
version = "0.1.0"
edition = "2024"

[dependencies]
defmt = { version = "1.1.0", features = [], optional = true }
serde = { version = "1.0.228", features = [], optional = true }

[features]
default = []
serde = [
    "dep:serde",
    "serde/std",
    "serde/derive"
]
defmt = ["dep:defmt"]